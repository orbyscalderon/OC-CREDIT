# =============================================================================
# OC CREDIT FRONTEND - Variables de Entorno
# =============================================================================
#
# PRODUCCIÓN (Vercel):
#   No se necesita ninguna variable. El vercel.json ya tiene el rewrite:
#   /api/* → http://104.207.144.76/api/*
#
# DESARROLLO LOCAL:
#   No se necesita tampoco. vite.config.ts tiene el proxy:
#   /api → http://localhost:4000
#
# Si en el futuro cambias la URL del backend en producción, actualiza
# frontend/vercel.json (campo "destination") y redespliega en Vercel.
