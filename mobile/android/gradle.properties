org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# La ruta del proyecto tiene una "o" con tilde (usuario de Windows). Gradle
# bloquea el build por defecto en rutas no-ASCII; esto lo fuerza a intentarlo
# igual (funciona salvo que alguna dependencia compile codigo nativo C/C++).
android.overridePathCheck=true
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
